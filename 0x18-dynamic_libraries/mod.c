#include <stdlib.h>
#include <stdio.h>

/**
* mod - a function that returns the module of 2 ints
* @k: int 1
* @v: int 2
* Return: final answer of the mod
*/
int mod(int k, int v)
{
	return (k % v);
}
