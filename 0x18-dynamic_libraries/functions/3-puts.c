#include "main.h"
/**
* _puts - a program that prints a string to stdout
* @s: string printer
* _putchar prints a new line
*/
void _puts(char *s)
{
	while (*str)
		_putchar(*str++);
	_putchar('\n');
}
