#include "main.h"
/**
* _abs - a program computing the absolute value of an int
* @n: int to be checked
* Return: integer absolute value
*/
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
