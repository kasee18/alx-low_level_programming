#include "main.h"
/**
* _islower - a program that checks for lowercase character
* @c: character to check
* Return: 1 if c is a lowercase, and 0 if else
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
